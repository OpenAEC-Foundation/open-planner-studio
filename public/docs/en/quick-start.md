# Quick start

**PLACEHOLDER CONTENT (wave 1).** This article exists only to test the help viewer end to end —
headings, formatting, lists, links and an image — before the real quick-start guide is written in
a later wave. Do not treat the text below as planning instructions.

This page shows *every* supported formatting form of the mini-markdown parser at a glance, so the
render path is demonstrably working: **bold text**, *italic text*, inline code such as
`runCPM()`, code blocks, ordered and unordered lists, an internal link and an example link.

## Formatting examples

A normal paragraph with some **bold**, some *italic*, and a bit of `inline code` in the middle.
Multiple paragraphs are separated by a blank line, exactly like regular markdown.

This is the second paragraph of the same section, purely to confirm paragraph separation works.

### Code block

```
function example() {
  return "this is a code block — rendered literally, no formatting parsed inside";
}
```

### Unordered list

- First item
- Second item with **bold** inside
- Third item with `inline code` inside

### Ordered list

1. Open the File menu
2. Choose Examples
3. Click an example project to open it

## References

Also read the [Planning & WBS](docs://gids-plannen-wbs) guide — this is an internal `docs://`
link that navigates within the viewer, not to an external page.

Follow the steps above in a real project: open the
[Renovation & Extension Single-Family Home](examples://showcase-verbouwing-eengezinswoning.ifc)
example — this is an `examples://` link that uses the same open path as File → Examples.

![Screenshot: Gantt chart with a calculated schedule (PLACEHOLDER, becomes a real screenshot in
wave 2+)](screenshots/placeholder-gantt.png)
