/// <reference types="vite/client" />

declare const __APP_VERSION__: string;
/** Per-worktree dev instance slug, or '' in a plain build (see vite.config.ts). */
declare const __OPS_DEV_INSTANCE__: string;
